{
  programs.zathura = {
    enable = true;
    extraConfig = ''
      # copy selection to system clipboard
      set selection-clipboard clipboard
      # theme
      set default-fg "#282a36"
    '';
  };
}
